/**
 * ARQUIVO GERADO AUTOMATICAMENTE pelo kysely-codegen.
 *
 * NÃO EDITE ESTE ARQUIVO MANUALMENTE.
 *
 * Para regenerar após criar ou alterar migrations:
 *   1. Suba o banco: docker compose up -d
 *   2. Aplique as migrations: bun run migrate:up
 *   3. Regenere os tipos: bun run db:codegen
 *
 * O comando lê DATABASE_URL do arquivo .env e conecta ao banco
 * para introspectar o schema e gerar as interfaces TypeScript.
 *
 * Referência: https://github.com/RobinBlomberg/kysely-codegen
 */

// Os tipos abaixo serão gerados automaticamente após rodar bun run db:codegen
// com o banco em execução. Até lá, este arquivo serve como placeholder.
export type DB = Record<string, unknown>
